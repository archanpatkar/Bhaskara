x := !(T | F) & ~F & 5 > 5
x = (-500) * 500 + 6

// type Color = Red | Blue | Green

def f1(x) = x^10
def f2(y) {
    z := x + f1(y)*10
    sin((z & false)+1)
}
print(f2(10))

k := [10,20,30,40]
j := [1,2,3]
print(mean(k))

z := (@x in k,#y in j) holds p(x) & m(y) | m(x) 

x := (#f) in k holds m(f)

def f3(x) = {
    match x
    case 10 then x 
    case 0 then (x+3)*3
    default x+2 
}